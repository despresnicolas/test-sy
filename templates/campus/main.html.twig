{% extends 'base.html.twig' %}

{% block body %}
    {% set array = [] %}
    {% for site in campus %}
        {% set array = array|merge([{campus:site.nom}]) %}
    {% endfor %}

    {% set vars = {values: array, title: 'campus'} %}

    {% include 'includes/table.html.twig' with vars only %}

{% endblock %}

{% block title %}{% endblock %}


